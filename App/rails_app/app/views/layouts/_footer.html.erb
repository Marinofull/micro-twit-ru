<% if signed_in? %>
  <footer class="text-center">
    <div class="col-xs-4">
      <%= link_to "", root_path, class: "glyphicon glyphicon-home" %>
    </div>
    <div class="col-xs-4">
      <%= link_to "", new_micropost_path, class: "glyphicon glyphicon-send" %>
    </div>
    <div class="col-xs-4">
      <%= link_to "", edit_user_path(current_user), class: "glyphicon glyphicon-user" %>
    </div>
  </footer>
<% else %>
  <footer class="footer">
  </footer>
<% end %>
